// http协议
// curl  相当于get请求
// DNS解析; preFetch 预解析 ；把域名解析成IP地址
// TCP协议断开链接必须4次吗？
// TCP 协议  因为有半关闭状态；
// ajax  fetch  Restful API
// 跨域，浏览器禁止跨域，和HTTP协议没有关系；
// 前端jsonp，JavaScript解析的机制，返回一个自运行函数，服务端解析；  运维端nginx代理

// SSH HTTPS 都是非对称加密；

// server 长度的计算根据Content-Type 'image/jpeg'
// res.retHeader(Content-Type 'image/jpeg');
// res.retHeader('Content-Length' :Buffer.byteLength(body));

// MTU 最大传输单元；帧一般为1500 byte；传多少取决于短板原理；
// 断点续传就是切割文件；

// ifconfig port  查看接口信息；
// netstat -r  router
// OSS 阿里云的对象存储；来优化网站；
// 1.一般都是静态的有高速缓存；静态服务器发送比较快；
// 2.没有cookie
// CDN 从最近的服务器获取资源；

// H5开源的播放器；