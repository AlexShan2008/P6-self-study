{
  "name": "redis",
  "version": "1.0.0",
  "description": "## 1 `mySQL` 关系型数据库，存的表，表里存的记录行 磁盘数据库，性能可靠Oracal",
  "main": "1.redis.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "redis": "^2.8.0"
  }
}
